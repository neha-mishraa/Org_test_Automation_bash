pipelines:
  - name: S_Bash_Generic_8610_007_pipe
    steps:
      - name: S_Bash_Generic_8610_007_step
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "onExecute"
          onSuccess:
            - echo "onSuccess"
          onComplete:
            - badcommand
