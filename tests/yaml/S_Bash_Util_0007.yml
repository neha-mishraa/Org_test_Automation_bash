pipelines:
  - name: pipeline_S_Bash_Util_0007
    steps:
      - name: S_Bash_Util_0007
        type: Bash
        execution:
          onExecute:
            - mkdir -p emptyDirectory
            - save_tests emptyDirectory
